VITE_API_URL=http://localhost:5001

# NOTE: NEVER put secrets here. Vite injects variables starting with VITE_
# directly into the BUILT client JS bundle — ANYONE can read them in DevTools.
#
# All API keys (GEMINI, VOYAGE, DB passwords, JWT secrets) live ONLY in:
#   _backend/.env    (server-side only, NEVER sent to browser)
#
# If you previously had secrets here, MOVE THEM TO _backend/.env NOW.
